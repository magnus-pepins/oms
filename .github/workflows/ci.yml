name: OMS CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Warm primary DB image so Testcontainers does not fail the Gradle "Test" step on first-pull
      # latency or anonymous Docker Hub rate limits before tests start.
      - name: Pre-pull container images used in tests
        run: docker pull postgres:16-alpine

      - name: Test
        run: ./gradlew clean test --no-daemon --no-build-cache --stacktrace

      - name: Assemble
        run: ./gradlew bootJar --no-daemon --no-build-cache
